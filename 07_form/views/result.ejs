<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>form 전송 결과</title>
  </head>
  <body>
    <!-- {
        title: "GET",
        userInfo: req.query, // 생김샘 {id: "", email: "", password: ""}
      } -->
    <h1><%= title%>요청 확인 하기</h1>
    <div>
      <span><%= userInfo.id%>(<%= userInfo.email%>)</span>님 환영합니다!
    </div>
    <div>비밀번호는 <span><%= userInfo.password%></span>이군요!!</div>
    <a href="/">홈으로 이동</a>
    <%#for() ejs 주석 처리 방법%>

    <h1><%= title%>요청 확인 하기</h1>
    <div>
      <span><%= userInfo.id2%>(<%= userInfo.email2%>)</span>님 환영합니다!
    </div>
    <div>비밀번호는 <span><%= userInfo.pw2%></span>이군요!!</div>
    <a href="/">홈으로 이동</a>

    <br />
    <h1><%= title%>요청 확인 하기 실습!!!</h1>
    <div>
      <span><%= userInfo.idpractice%>(<%= userInfo.idpractice%>)</span>님 실습
      중입니다!
    </div>
    <div>성별: <span><%= userInfo.gender%></span>이군요</div>
    <div>
      생년월일: <span><%= userInfo.year%></span>년<span
        ><%= userInfo.month%></span
      >월<span><%= userInfo.days%></span>일
    </div>
    <div>관심사: <span><%= userInfo.trip%></span></div>
    <a href="/">홈으로 이동</a>
  </body>
</html>
