<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>socket.io 실습1번</title>
    <script src="/socket.io/socket.io.js"></script>
  </head>
  <body>
    <h1>실습1번</h1>
    <button onclick="hello()">hello</button>
    <button onclick="study()">study</button>
    <button onclick="bye()">bye</button>
    <h3></h3>

    <script>
      const socket = io();
      const h3 = document.querySelector("h3");

      function hello() {
        socket.emit("hello", "hello", (message) => {
          h3.textContent = "server: " + message;
        });
      }

      function study() {
        socket.emit("study", "study");
        socket.on("study2", (message) => {
          h3.textContent = "server: " + message;
        });
      }

      function bye() {
        socket.emit("bye", "bye");
        socket.on("bye2", (message) => {
          h3.textContent = "server: " + message;
        });
      }
    </script>
  </body>
</html>

<!-- <!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="/socket.io/socket.io.js"></script>
  </head>
  <body>
    <h1>실습1번</h1>
    <button onclick="hello()">hello</button>
    <button onclick="study()">study</button>
    <button onclick="bye()">bye</button>
    <h3></h3>

    <script>
      const scoket = io();
      const h3 = document.querySelector("h3");

      function hello() {
        // scoket.emit("hello", "hello", (message) => {
        //   h3.textContent = "server:" + message;
        // });
        scoket.emit("hello", "hello");
        scoket.on("hello2", (message) => {
          h3.textContent = "server: " + message;
        });
      }
      function study() {
        scoket.emit("study", "study")
        scoket.on("hello2", (message) => {
          h3.textContent = "server: " + message;
        });
      }
      function bye() {
        scoket.emit("bye", "bye", (message) => {
          h3.textContent = "server:" + message;
        });
      }
    </script>
  </body>
</html> -->
